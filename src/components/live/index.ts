/**
 * live_event components (CONVERGENCE PART II, S2.5 slice).
 * The S2 rendering lane imports the card + strip from here.
 */
export { default as LiveEventCard, LiveNowStrip } from "./LiveEventCard";
