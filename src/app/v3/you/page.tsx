import AppShell from "@/ui-v3/components/AppShell";
import ScreenPlaceholder from "@/ui-v3/components/ScreenPlaceholder";

/** /v3/you — placeholder so the bottom nav is fully navigable. */
export default function V3YouPage() {
  return (
    <AppShell>
      <ScreenPlaceholder name="You" />
    </AppShell>
  );
}
