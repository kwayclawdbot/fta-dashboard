import AppShell from "@/ui-v3/components/AppShell";
import ScreenPlaceholder from "@/ui-v3/components/ScreenPlaceholder";

/** /v3/discover — placeholder so the bottom nav is fully navigable. */
export default function V3DiscoverPage() {
  return (
    <AppShell>
      <ScreenPlaceholder name="Discover" />
    </AppShell>
  );
}
