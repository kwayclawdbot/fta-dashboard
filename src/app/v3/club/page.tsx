import AppShell from "@/ui-v3/components/AppShell";
import ScreenPlaceholder from "@/ui-v3/components/ScreenPlaceholder";

/** /v3/club — placeholder so the bottom nav is fully navigable. */
export default function V3ClubPage() {
  return (
    <AppShell>
      <ScreenPlaceholder name="Club" />
    </AppShell>
  );
}
