import LearnSurface from "@/components/learn/LearnSurface";

/**
 * /courses — the LEARN surface ("Grow your edge"), the Learn nav slot's home.
 *
 * Rebuilt onto the F0 vocabulary: one obsidian "continue learning" field, a
 * Journey / Classes / Missions rail, and hairline ledgers everywhere else. No
 * card grids, no cover-art tiles. Free-tier sampler, tier locks and the kid
 * register are all preserved inside the surface.
 */
export default function CoursesPage() {
  return <LearnSurface />;
}
