import ProfileSurface from "@/components/you/ProfileSurface";

/**
 * /progress — the member's PROFILE surface ("You"), the 5th nav slot's home.
 *
 * Rebuilt onto the F0 vocabulary: one obsidian identity hero, hairline ledgers,
 * section rules, and a measure strip. No card grids. Every number is a real
 * own-user read; anything the feed can't supply renders "—".
 */
export default function ProgressPage() {
  return <ProfileSurface />;
}
