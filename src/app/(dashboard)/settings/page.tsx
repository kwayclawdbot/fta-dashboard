import SettingsSurface from "@/components/settings/SettingsSurface";

/**
 * /settings — rebuilt onto the F0 vocabulary: section rules + hairline ledger
 * rows, no settings-cards. Theme, notification prefs, membership and sign-out
 * are all live controls, not display.
 */
export default function SettingsPage() {
  return <SettingsSurface />;
}
