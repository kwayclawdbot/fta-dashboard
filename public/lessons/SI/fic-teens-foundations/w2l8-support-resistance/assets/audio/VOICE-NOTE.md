# Voice note — Support & Resistance: The Floor and the Ceiling

Narration rendered on **Kway Voicebox (Qwen TTS, localhost:17493)** as an interim voice.
OpenAI TTS quota was exhausted (429) at build time. When quota is restored, re-record on
`gpt-4o-mini-tts` voice `coral` (canonical teens voice per built pipeline) using the section
scripts in `./script/` (one file per section, in order), then regenerate timings.

Sections: 6 · total 158.6s · 0.5s gaps between sections.
